
<%= render partial: 'dashboard/nav'%>


<div class="main-panel">
      <!-- Navbar -->
      <nav class="navbar navbar-expand-lg navbar-absolute fixed-top navbar-transparent">
        <div class="container-fluid">
          <div class="navbar-wrapper">
            <div class="navbar-toggle">
              <button type="button" class="navbar-toggler">
                <span class="navbar-toggler-bar bar1"></span>
                <span class="navbar-toggler-bar bar2"></span>
                <span class="navbar-toggler-bar bar3"></span>
              </button>
            </div>
            <a class="navbar-brand" href="javascript:;">Clientes Cadastrados</a>
            <%= link_to "Voltar Página Principal", root_path ,class:'btn btn-primary btn-round'%>
            <% if admin_signed_in? %>
  <%#= link_to 'Adicionar Produto', new_client_path,class:'btn btn-success btn-round' %> 
<% end %>
          
              
          </div>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navigation" aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-bar navbar-kebab"></span>
            <span class="navbar-toggler-bar navbar-kebab"></span>
            <span class="navbar-toggler-bar navbar-kebab"></span>
          </button>
        
        </div>
      </nav>
      <!-- End Navbar -->
      <div class="content">
        <div class="row">

         <% @clients.each do |client| %>
   <div class="col-lg-6 col-md-6 col-sm-6">
            <div class="card card-stats">
              <div class="card-body ">
                <div class="row">
                  <div class="col-5 col-md-4">
                    <div class="icon-big text-center icon-warning">
                    <%#= image_tag client.image, style:"width:150px;" if client.image.attached? %>
                    </div>
                  </div>
                    
                  <div class="col-7 col-md-8">
                    <div class="numbers">
                      <p class="card-category"><%= client.nome %></p>
                      
                      <p class="card-title"><%= client.saldo_dotz %> dotz</p><p>
                    </p></div>
                  </div>
                </div>
              </div>

           <div class="card-footer ">
                <hr>
                <div class="stats">
                  
                  
        <% if admin_signed_in? %>
        <td><%= link_to 'Adicionar Dotz', client_edit_dotz_path(client), method: :patch ,class:'btn btn-warning btn-round'%></td>
          <td><%= link_to 'Visualizar', client_show_path(client),class:'text-muted' %></td>
          
          <td><%= link_to 'Deletar', clients_delete_path(client), method: :delete, data: { confirm: 'Tem certeza disso?' }, class:'text-danger' %></td>
        <% else %>
        <td><%#= link_to 'Comprar Produto', new_client_purchase_path(client),class:'btn btn-success btn-round' %></td>
        <%end%>
      </tr>
                </div>
              </div>

            </div>
          </div>
     
       
    <% end %>
      
                  


       





          
        </div>
        
      </div>
     
        
    </div>
   

