
<% if user_signed_in? && current_user.admin %>
    <h1>Dashboard Admin</h1>
    <%= link_to "Sair do Admin", destroy_user_session_path, method: :delete, data: { confirm: "Tem certeza que quer sair? "} %>
    <%= link_to "Meu Perfil", edit_user_registration_path %>
<% elsif user_signed_in? && current_user.client %>
    <h1>Dashboard Client</h1>
    <%= link_to "Sair da Dashboard", destroy_user_session_path, method: :delete, data: { confirm: "Tem certeza que quer sair? "} %>|
    <%= link_to "Meu Perfil", edit_user_registration_path %>
<% else %>
    <h1>Logue na plataforma e veja o que tem dentro!!</h1>
<% end %>

<p>Bem Vindo a Nossa Loja Dotz!!</p>

<% unless user_signed_in? %>
    <%= link_to "Login", new_user_session_path %>
<br><br>
<% end %>

<%= link_to "Ver produtos da Loja", products_path %>

